pluginManagement {
    repositories {
        google()
        mavenCentral()
        gradlePluginPortal()
    }
}

rootProject.name = 'mendikotgame'

include ':app'

includeBuild('../node_modules/@react-native/gradle-plugin')
